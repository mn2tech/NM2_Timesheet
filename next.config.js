/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/nm2timesheet',
  images: {
    unoptimized: true,
  },
  // Optional: If you're using a trailing slash
  // trailingSlash: true,
}

module.exports = nextConfig


